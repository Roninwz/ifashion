<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zua.ifashion.person.mapper.RankMapper">
  <resultMap id="BaseResultMap" type="com.zua.ifashion.person.entity.Rank">
    <id column="rankId" jdbcType="INTEGER" property="rankId" />
    <result column="rankName" jdbcType="VARCHAR" property="rankName" />
  </resultMap>


  <!-- 添加   -->
  <insert id="addRank" parameterType="com.zua.ifashion.person.entity.Role">
    insert into role(roleId,roleName) values(#{roleId},#{roleName})
  </insert>

  <!-- 删除    -->
  <delete id="deleteRank" parameterType="int">
    delete from role where roleId = #{roleId}
  </delete>

  <!-- 修改   -->
  <update id="updateRank" parameterType="com.zua.ifashion.person.entity.Role">
    update  role set roleName = #{roleName}
  </update>


  <select id="selectRankByRoleId" parameterType="int" resultType="com.zua.ifashion.person.entity.Role">
    select *  from role where roleId =#{roleId}
  </select>

  <!-- 查询所有    -->
  <select id="getAllRoles"  resultType="com.zua.ifashion.person.entity.Role">
    select * from role
  </select>



  <!-- 查询所有数量    -->
  <select id="getAllRoleCount"  resultType="int">
    select count(*) from role
  </select>



</mapper>